Dark-well input with a cyan focus ring; use `variant="search"` for the static site search (leading `/` prompt, mono text).

```jsx
<Input placeholder="Title…" />
<Input variant="search" placeholder="Search the index…" />
```

Default uses body font for form fields; `search` switches to mono and shows the prompt glyph. Focus lights the border + soft glow.
