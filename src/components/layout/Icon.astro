---
/**
 * Inline Lucide icons (MIT). The design system specifies Lucide for its 1.5px
 * stroke / no-fill geometry; we embed the handful of glyphs the UI uses as
 * inline SVG rather than loading the Lucide CDN, so reading pages stay zero-JS
 * and the CSP can keep `script-src 'self'`.
 */
import { ICONS } from "../../utils/icons";

interface Props {
  name: string;
  size?: number;
  class?: string;
  color?: string;
}
const { name, size = 18, class: cls, color } = Astro.props;

const inner = ICONS[name] ?? "";
---

<svg
  xmlns="http://www.w3.org/2000/svg"
  width={size}
  height={size}
  viewBox="0 0 24 24"
  fill="none"
  stroke={color ?? "currentColor"}
  stroke-width="1.5"
  stroke-linecap="round"
  stroke-linejoin="round"
  class={cls}
  aria-hidden="true"
  set:html={inner}
/>
