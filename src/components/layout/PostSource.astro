---
import TopbarChip from "./TopbarChip.astro";
import { postSourceUrl, type Post } from "../../utils/posts";

/** The MDX behind this entry, on GitHub. */
interface Props {
  post: Post;
}
---

<TopbarChip
  href={postSourceUrl(Astro.props.post)}
  icon="github"
  label="view source"
/>
